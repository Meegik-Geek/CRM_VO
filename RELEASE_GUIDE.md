# 🚀 Гайд з випуску оновлень CRM "Вступ.Офіс" через GitHub

Цей посібник допоможе вам правильно випустити оновлення, щоб клієнти могли завантажити його автоматично.

## 1. Підготовка нової версії
1.  **Оновіть код**: Внесіть необхідні зміни в проект.
2.  **Змініть версію**:
    *   Відкрийте файл `version.txt` і змініть номер версії (наприклад, з `1.0.2` на `1.0.3`).
    *   Відкрийте `setup.py` і змініть поле `version="1.0.3"`.
3.  **Виконайте збірку**:
    ```bash
    python setup.py build
    ```

## 2. Створення архіву оновлення
1.  Перейдіть у папку `build/exe.win-amd64-3.14/` (або аналогічну).
2.  **Виділіть усі файли та папки** всередині неї.
3.  Створіть **ZIP-архів** з назвою `update.zip`. 
    *   *Важливо: Архів повинен містити самі файли програми (main.exe, lib, resource тощо), а не папку з ними.*

## 3. Публікація на GitHub
1.  Зайдіть у ваш репозиторій на GitHub.
2.  Перейдіть у розділ **Releases** ➡ **Draft a new release**.
3.  Вкажіть **Tag version** (наприклад, `v1.0.3`).
4.  Вкажіть **Release title** (наприклад, `Оновлення 1.0.3`).
5.  В описі (Release notes) розпишіть, що змінилося.
6.  **НАЙВАЖЛИВІШЕ**: У поле **Attach binaries** (скріпка) перетягніть ваш створений файл `update.zip`.
7.  Натисніть **Publish release**.

## 4. Налаштування в CRM (Адмін-панель)
Після публікації на GitHub:
1.  Зайдіть у CRM під адміністратором.
2.  Перейдіть у розділ **Оновлення**.
3.  Система автоматично побачить нову версію на GitHub.
4.  Натисніть **Оновити та перезавантажити** (на клієнтських ПК) або **Розповсюдити через Інтернет**, щоб усі комп'ютери в мережі побачили це оновлення.

---

### ⚠️ Важливі зауваження:
*   **GITHUB_REPO**: Переконайтеся, що у файлі `.env` правильно вказано ваш репозиторій: `GITHUB_REPO=ваше_ім'я/назва_проекту`.
*   **Файл оновлювача**: Якщо ви змінюєте логіку самого `updater.py`, переконайтеся, що новий `updater.exe` також включено в `update.zip`.
*   **Публічний репозиторій**: Оновлення через GitHub працює найпростіше, якщо репозиторій публічний. Якщо він приватний, знадобиться GitHub Token (це складніше налаштування).
